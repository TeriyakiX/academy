/*
 | Заглушка вместо старой отправки форм.
 |
 | Раньше этот файл отправлял заявки из браузера прямо в Telegram,
 | и токен бота лежал в нём открытым текстом — файл отдаётся всем,
 | кто откроет сайт. Теперь заявки принимает сервер: формы уходят
 | обычным POST на /lead, а токен хранится в переменных окружения.
 |
 | Файл оставлен, потому что его подключают старые сборки страниц:
 | без него импорт упал бы с ошибкой. Экспорты сохранены, но ничего
 | не делают — маску телефона и проверку полей взял на себя
 | resources/src/modules/lead.
 */

const noop = () => {};

export { noop as a, noop as b, noop as s };
