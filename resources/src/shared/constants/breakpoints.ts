/*
 | Ширины экрана. Те же значения стоят в стилях — держим их
 | в одном месте, чтобы вёрстка и поведение не разъезжались.
 */
export const MOBILE_MAX = 760;
export const TABLET_MAX = 1000;

export const MEDIA_MOBILE = `(max-width: ${MOBILE_MAX}px)`;
export const MEDIA_TABLET = `(max-width: ${TABLET_MAX}px)`;
