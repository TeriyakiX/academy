{{--
    Защита форм от автоматических отправок.

    1. Ловушка (honeypot): поле, скрытое от людей, но видимое ботам.
       Бот заполняет все поля подряд — если оно не пустое, заявка отбрасывается.
       Прячем не через display:none (это боты научились определять),
       а уводим за пределы экрана и убираем из обхода клавиатурой.

    2. Отметка времени: боты отправляют форму мгновенно.
       Заявки быстрее порога считаем автоматическими.
--}}
<div class="ab-guard" aria-hidden="true">
    <label>
        Не заполняйте это поле
        <input type="text" name="website" tabindex="-1" autocomplete="off" value="">
    </label>
</div>

<input type="hidden" name="loaded_at" value="{{ encrypt(now()->timestamp) }}">
