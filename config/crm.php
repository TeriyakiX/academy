<?php

/*
 | Связь с CRM академии.
 |
 | Курсы, цены и тексты сайт берёт из CRM, заявки отправляет туда же.
 | Пока адрес или ключ не заданы, сайт работает на своих файлах
 | настроек, как раньше.
 */
return [
    // Например https://oh3.ru/api/site/v1
    'url'   => env('CRM_API_URL'),
    'token' => env('CRM_API_TOKEN'),

    // Сколько секунд ждать ответа CRM, прежде чем взять запасную копию.
    'timeout' => (int) env('CRM_API_TIMEOUT', 3),

    // Как часто обновлять данные из CRM, секунды.
    'ttl' => (int) env('CRM_API_TTL', 300),
];
