<?php

/*
 | Адреса для поисковиков.
 |
 | Боевой домен задан явно: canonical и sitemap должны указывать на него
 | и со тестового сайта тоже — иначе поисковик решит, что это два разных
 | сайта с одинаковым содержимым, и разделит позиции между ними.
 */

return [
    'domain' => env('SEO_DOMAIN', 'https://academy-barista.ru'),

    /*
     | Хосты, которым индексация запрещена. Тестовая копия не должна
     | попадать в выдачу: она мешает боевому сайту.
     */
    'noindex_hosts' => [
        'acti-barista.ru',
        'www.acti-barista.ru',
        'localhost',
        '127.0.0.1',
    ],

    /* Разделы, которых в поиске быть не должно. */
    'disallow' => [
        '/up',              // служебная проверка живости Laravel
        '/thank-you.html',  // страница после отправки заявки
        '/*?*etext=',
        '/*?*from=mainpage',
    ],
];
